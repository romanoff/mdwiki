{
  "$schema": "http://json-schema.org/schema",
  "type": "array",
  "description": "A list of documents",
  "minItems": 0,
  "items": {
    "type": "object",
    "description": "document",
    "properties": {
      "id": {"type": "string"},
      "name": {"type": "string"},
      "space_id": {"type": "string"}
    },
    "required": ["id", "name", "space_id"]
  }
}
